{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "paos://schemas/events/kernel.task.failed.v1.schema.json",
  "title": "kernel.task.failed payload",
  "description": "P-M3-2 (doc 19) — doc 05 §3.1 định sẵn tên/payload, TaskStore.fail() là caller thật đầu tiên.",
  "type": "object",
  "additionalProperties": false,
  "required": ["task_id", "step_id", "attempt"],
  "properties": {
    "task_id": { "type": "string" },
    "step_id": { "type": "string" },
    "attempt": { "type": "integer", "minimum": 0 }
  }
}
