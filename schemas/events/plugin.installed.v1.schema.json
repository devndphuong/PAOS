{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "paos://schemas/events/plugin.installed.v1.schema.json",
  "title": "plugin.installed payload",
  "description": "doc 12 §2, P-M8-2 — plugin vừa cài xong (validate manifest + duyệt quyền + copy/clone vào workspace/plugins/, ADR-0018), Registry hot-reload.",
  "type": "object",
  "additionalProperties": false,
  "required": ["plugin_id", "version"],
  "properties": {
    "plugin_id": { "type": "string" },
    "version": { "type": "string" }
  }
}
