{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "paos://schemas/events/plugin.removed.v1.schema.json",
  "title": "plugin.removed payload",
  "description": "doc 12 §2, P-M8-2 — `paosctl plugin uninstall` đã gỡ code plugin khỏi workspace/plugins/ (giữ nguyên Project/Artifact plugin đã tạo).",
  "type": "object",
  "additionalProperties": false,
  "required": ["plugin_id", "version"],
  "properties": {
    "plugin_id": { "type": "string" },
    "version": { "type": "string" }
  }
}
